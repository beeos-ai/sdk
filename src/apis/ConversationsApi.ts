/* tslint:disable */
/* eslint-disable */
/**
 * BeeOS OpenAPI (user contract)
 * The **BeeOS OpenAPI** user contract. The sole implementer is **[`services/openapi-gateway`](../services/openapi-gateway)** (`openapi-gw`, local `:8095`). SDKs (Go / TypeScript) published under [`sdks/beeos-ai-sdk-go`](../../sdks/beeos-ai-sdk-go) and [`sdks/beeos-ai-sdk`](../../sdks/beeos-ai-sdk) are generated directly from this document.  The main user Gateway (`services/gateway`, `:9080`) serves web / mobile / desktop clients and is **not** described by this document. It maintains its own route set independent of the SDK contract.  **Auth:** every operation requires either a user **JWT** or a **`oag_`** User API Key, both on `Authorization: Bearer`. Agent-level protocol endpoints (A2A JSON-RPC with `bak_`, MCP) live in [beeos-agent-integration-v1.yaml](beeos-agent-integration-v1.yaml) and are hosted by A2A Gateway / MCP Gateway — not by `openapi-gateway`.  ## Changelog  ### 1.1.0 (ADR-0022 — dual-layer message storage)  BREAKING for clients that depended on full streaming chunk replay via the persistent message log:  * `GET /api/v1/agents/{agentId}/conversations/{convId}/messages`   and `GET /api/v1/agents/{agentId}/tasks/{taskId}/messages` now   **default-filter out** ephemeral streaming chunks   (`agent_reply_delta`, `agent_thought_chunk`,   `agent_message_chunk`). Add `?include_deltas=true` to opt back in. * `latest_offset` continues to reflect the TRUE server-side max   offset across both filtered and unfiltered rows, so `since=` /   cursor-style pagination is unaffected. * SSE `/events` is **NOT** affected — live consumers continue to   receive every envelope as it happens. * Backwards-compatible recovery path: SSE `/events` may now emit   a `backfill_truncated` event frame when the client reconnects   with `Last-Event-ID` before the oldest retained chunk. SDKs   that don\'t recognise the frame should treat it as a hint to   resume from `oldest_redis_offset` instead (the field carrying   the smallest offset still readable on the per-channel stream). * **Clarified contract**: SSE event `offset` is strictly monotonic   per channel but **NOT guaranteed contiguous** (ADR-0022 §1.2).   Producer-side write failures may leave small holes; clients MUST   use `offset > since`, never `offset == since + 1`.  BREAKING for clients that depended on the `oag_` API key scope vocabulary:  * The per-route scope gate (`agents:read`, `agents:write`,   `tasks:read`, `tasks:write`, `files:read`, `files:write`,   `instances:read`, `instances:write`, plus the `admin:*` wildcard)   has been **removed entirely**. The 37 routes that previously   required scopes are now governed exclusively by owner-ACL — an   `oag_` User API Key inherits full access to its owner\'s resources. * The `403 insufficient_scope` error code is no longer emitted.   Existing callers that branched on it should fold the case into   their generic 403 / `forbidden` handler. * SDK clients that explicitly passed `scopes` to `createAPIKey`   should drop that argument. The Web / Desktop UIs no longer render   scope badges on existing keys. * Existing `oag_` keys continue to work unchanged — the owner_id   binding is preserved, and no key needs to be re-issued.  BREAKING for clients of `GET /api/v1/providers` that read the `capabilities` object on the wire:  * 7 of the 8 `capabilities` field names are now **camelCase**   (`longRunning`, `browserUse`, `codeExec`, `fileSystem`,   `customImage`, `maxDurationSec`, `costModel`) — matching this   OpenAPI contract. Previous wire emitted **snake_case**   (`long_running`, `browser_use`, …) because the server DTO bound   `capabilities` to the raw protobuf message   (`*pb.ProviderCapabilities`) whose auto-generated `json` tags   use snake_case. SDK consumers (`@beeos-ai/sdk`,   `github.com/beeos-ai/sdk-go`) generated from this spec were   **already unable** to read any `capabilities` field — the   camelCase property names produced by the generator never   matched the snake_case the server actually sent. Raw HTTP   callers that hand-coded against the old snake_case names must   migrate; SDK callers gain access to the field set for the first   time without code changes. * Note: the `device` boolean was always a single word — its wire   name (`device`) is unchanged. Only the seven multi-word   capability fields are affected by the rename. * `meta`, `id`, `name`, `description`, `version` are unchanged. * A new drift-guard (`internal/dto/contract_test.go`) now locks   the server DTO to the spec property names, so the two cannot   silently diverge again.  ### 1.0.0  Initial OpenAPI contract (ADR-001). 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CancelConversation202Response,
  CancelConversationRequest,
  ClearConversation202Response,
  ConversationResponse,
  CreateConversationRequest,
  ErrorResponse,
  ListConversationsResponse,
  ListMessagesResponse,
  RenameConversationRequest,
  SendMessageRequest,
  SendMessageResponse,
  SetConversationModelRequest,
} from '../models/index';
import {
    CancelConversation202ResponseFromJSON,
    CancelConversation202ResponseToJSON,
    CancelConversationRequestFromJSON,
    CancelConversationRequestToJSON,
    ClearConversation202ResponseFromJSON,
    ClearConversation202ResponseToJSON,
    ConversationResponseFromJSON,
    ConversationResponseToJSON,
    CreateConversationRequestFromJSON,
    CreateConversationRequestToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ListConversationsResponseFromJSON,
    ListConversationsResponseToJSON,
    ListMessagesResponseFromJSON,
    ListMessagesResponseToJSON,
    RenameConversationRequestFromJSON,
    RenameConversationRequestToJSON,
    SendMessageRequestFromJSON,
    SendMessageRequestToJSON,
    SendMessageResponseFromJSON,
    SendMessageResponseToJSON,
    SetConversationModelRequestFromJSON,
    SetConversationModelRequestToJSON,
} from '../models/index';

export interface CancelConversationOperationRequest {
    agentId: string;
    conversationId: string;
    cancelConversationRequest: CancelConversationRequest;
    idempotencyKey?: string;
    xBeeOSOperationId?: string;
}

export interface ClearConversationRequest {
    idempotencyKey: string;
    agentId: string;
    conversationId: string;
    xBeeOSOperationId?: string;
}

export interface CreateConversationOperationRequest {
    agentId: string;
    idempotencyKey?: string;
    createConversationRequest?: CreateConversationRequest;
}

export interface DeleteConversationRequest {
    agentId: string;
    convId: string;
}

export interface GetConversationRequest {
    agentId: string;
    convId: string;
}

export interface ListConversationMessagesRequest {
    agentId: string;
    convId: string;
    since?: number;
    limit?: number;
    includeDeltas?: boolean;
}

export interface ListConversationsRequest {
    agentId: string;
    state?: ListConversationsStateEnum;
    since?: string;
    limit?: number;
}

export interface RenameConversationOperationRequest {
    idempotencyKey: string;
    agentId: string;
    convId: string;
    renameConversationRequest: RenameConversationRequest;
}

export interface SendConversationMessageRequest {
    agentId: string;
    convId: string;
    sendMessageRequest: SendMessageRequest;
}

export interface SetConversationModelOperationRequest {
    idempotencyKey: string;
    agentId: string;
    conversationId: string;
    setConversationModelRequest: SetConversationModelRequest;
    xBeeOSOperationId?: string;
}

export interface StreamConversationEventsRequest {
    agentId: string;
    convId: string;
    since?: number;
}

/**
 * 
 */
export class ConversationsApi extends runtime.BaseAPI {

    /**
     * Cancel conversation execution
     */
    async cancelConversationRaw(requestParameters: CancelConversationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CancelConversation202Response>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling cancelConversation().'
            );
        }

        if (requestParameters['conversationId'] == null) {
            throw new runtime.RequiredError(
                'conversationId',
                'Required parameter "conversationId" was null or undefined when calling cancelConversation().'
            );
        }

        if (requestParameters['cancelConversationRequest'] == null) {
            throw new runtime.RequiredError(
                'cancelConversationRequest',
                'Required parameter "cancelConversationRequest" was null or undefined when calling cancelConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (requestParameters['xBeeOSOperationId'] != null) {
            headerParameters['X-BeeOS-Operation-Id'] = String(requestParameters['xBeeOSOperationId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{conversationId}/cancel`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"conversationId"}}`, encodeURIComponent(String(requestParameters['conversationId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CancelConversationRequestToJSON(requestParameters['cancelConversationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CancelConversation202ResponseFromJSON(jsonValue));
    }

    /**
     * Cancel conversation execution
     */
    async cancelConversation(requestParameters: CancelConversationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CancelConversation202Response> {
        const response = await this.cancelConversationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Clear conversation history
     */
    async clearConversationRaw(requestParameters: ClearConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ClearConversation202Response>> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling clearConversation().'
            );
        }

        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling clearConversation().'
            );
        }

        if (requestParameters['conversationId'] == null) {
            throw new runtime.RequiredError(
                'conversationId',
                'Required parameter "conversationId" was null or undefined when calling clearConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (requestParameters['xBeeOSOperationId'] != null) {
            headerParameters['X-BeeOS-Operation-Id'] = String(requestParameters['xBeeOSOperationId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{conversationId}/clear`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"conversationId"}}`, encodeURIComponent(String(requestParameters['conversationId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ClearConversation202ResponseFromJSON(jsonValue));
    }

    /**
     * Clear conversation history
     */
    async clearConversation(requestParameters: ClearConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ClearConversation202Response> {
        const response = await this.clearConversationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a long-lived dialog channel. Does NOT carry an initial message — callers post the first `chat_message` via `POST /conversations/{convId}/messages` once the conversation exists. 
     * Open a new conversation.
     */
    async createConversationRaw(requestParameters: CreateConversationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling createConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateConversationRequestToJSON(requestParameters['createConversationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationResponseFromJSON(jsonValue));
    }

    /**
     * Creates a long-lived dialog channel. Does NOT carry an initial message — callers post the first `chat_message` via `POST /conversations/{convId}/messages` once the conversation exists. 
     * Open a new conversation.
     */
    async createConversation(requestParameters: CreateConversationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationResponse> {
        const response = await this.createConversationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Closes the underlying MS channel. After close, no further messages can be posted but historical messages remain readable via `GET /messages`. 
     * Close a conversation.
     */
    async deleteConversationRaw(requestParameters: DeleteConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling deleteConversation().'
            );
        }

        if (requestParameters['convId'] == null) {
            throw new runtime.RequiredError(
                'convId',
                'Required parameter "convId" was null or undefined when calling deleteConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{convId}`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"convId"}}`, encodeURIComponent(String(requestParameters['convId'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Closes the underlying MS channel. After close, no further messages can be posted but historical messages remain readable via `GET /messages`. 
     * Close a conversation.
     */
    async deleteConversation(requestParameters: DeleteConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteConversationRaw(requestParameters, initOverrides);
    }

    /**
     * Get a single conversation\'s metadata.
     */
    async getConversationRaw(requestParameters: GetConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling getConversation().'
            );
        }

        if (requestParameters['convId'] == null) {
            throw new runtime.RequiredError(
                'convId',
                'Required parameter "convId" was null or undefined when calling getConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{convId}`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"convId"}}`, encodeURIComponent(String(requestParameters['convId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationResponseFromJSON(jsonValue));
    }

    /**
     * Get a single conversation\'s metadata.
     */
    async getConversation(requestParameters: GetConversationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationResponse> {
        const response = await this.getConversationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the durable conversation log. By default the response EXCLUDES ephemeral streaming chunks (`agent_reply_delta`, `agent_thought_chunk`, `agent_message_chunk`) — only finalized turns and tool messages are returned, so UIs render a clean transcript without pagination blowing up on token-level chunks.  Set `include_deltas=true` to also receive the per-token chunks (only present when an in-flight turn is still streaming). Note that `latest_offset` ALWAYS reflects the true server-side max offset across both filtered and unfiltered rows, so resuming with `since=<latest_offset>` is safe regardless of the filter.  SSE `/events` is unaffected by this filter — live consumers receive every envelope including deltas as they happen. 
     * List messages in a conversation (paged by offset).
     */
    async listConversationMessagesRaw(requestParameters: ListConversationMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListMessagesResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling listConversationMessages().'
            );
        }

        if (requestParameters['convId'] == null) {
            throw new runtime.RequiredError(
                'convId',
                'Required parameter "convId" was null or undefined when calling listConversationMessages().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['includeDeltas'] != null) {
            queryParameters['include_deltas'] = requestParameters['includeDeltas'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{convId}/messages`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"convId"}}`, encodeURIComponent(String(requestParameters['convId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListMessagesResponseFromJSON(jsonValue));
    }

    /**
     * Returns the durable conversation log. By default the response EXCLUDES ephemeral streaming chunks (`agent_reply_delta`, `agent_thought_chunk`, `agent_message_chunk`) — only finalized turns and tool messages are returned, so UIs render a clean transcript without pagination blowing up on token-level chunks.  Set `include_deltas=true` to also receive the per-token chunks (only present when an in-flight turn is still streaming). Note that `latest_offset` ALWAYS reflects the true server-side max offset across both filtered and unfiltered rows, so resuming with `since=<latest_offset>` is safe regardless of the filter.  SSE `/events` is unaffected by this filter — live consumers receive every envelope including deltas as they happen. 
     * List messages in a conversation (paged by offset).
     */
    async listConversationMessages(requestParameters: ListConversationMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListMessagesResponse> {
        const response = await this.listConversationMessagesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns long-lived dialog channels explicitly created via `POST /agents/{agentId}/conversations`. **Synchronous one-shot `POST /agents/{agentId}/invoke` calls do NOT appear here** — they create short-lived `protocol=openapi` task channels that surface under `GET /agents/{agentId}/tasks` and `GET /tasks` instead. Mixing the two would conflate \"long dialog\" with \"single RPC call\" and is intentionally avoided so list semantics stay stable per channel kind.  If `GET /conversations` returns an empty list immediately after an `invoke`, that is expected: call `GET /tasks` for the task channel record, or pre-create a conversation with `POST /conversations` and post messages into it explicitly. 
     * List conversations for the calling user against this agent.
     */
    async listConversationsRaw(requestParameters: ListConversationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListConversationsResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling listConversations().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['state'] != null) {
            queryParameters['state'] = requestParameters['state'];
        }

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListConversationsResponseFromJSON(jsonValue));
    }

    /**
     * Returns long-lived dialog channels explicitly created via `POST /agents/{agentId}/conversations`. **Synchronous one-shot `POST /agents/{agentId}/invoke` calls do NOT appear here** — they create short-lived `protocol=openapi` task channels that surface under `GET /agents/{agentId}/tasks` and `GET /tasks` instead. Mixing the two would conflate \"long dialog\" with \"single RPC call\" and is intentionally avoided so list semantics stay stable per channel kind.  If `GET /conversations` returns an empty list immediately after an `invoke`, that is expected: call `GET /tasks` for the task channel record, or pre-create a conversation with `POST /conversations` and post messages into it explicitly. 
     * List conversations for the calling user against this agent.
     */
    async listConversations(requestParameters: ListConversationsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListConversationsResponse> {
        const response = await this.listConversationsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rename a conversation through Message Service metadata authority.
     */
    async renameConversationRaw(requestParameters: RenameConversationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ConversationResponse>> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling renameConversation().'
            );
        }

        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling renameConversation().'
            );
        }

        if (requestParameters['convId'] == null) {
            throw new runtime.RequiredError(
                'convId',
                'Required parameter "convId" was null or undefined when calling renameConversation().'
            );
        }

        if (requestParameters['renameConversationRequest'] == null) {
            throw new runtime.RequiredError(
                'renameConversationRequest',
                'Required parameter "renameConversationRequest" was null or undefined when calling renameConversation().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{convId}`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"convId"}}`, encodeURIComponent(String(requestParameters['convId'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: RenameConversationRequestToJSON(requestParameters['renameConversationRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ConversationResponseFromJSON(jsonValue));
    }

    /**
     * Rename a conversation through Message Service metadata authority.
     */
    async renameConversation(requestParameters: RenameConversationOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ConversationResponse> {
        const response = await this.renameConversationRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Non-blocking — returns 202 with the persisted `message_id`. The agent reply lands later via the SSE `/events` stream or a follow-up `GET /messages?since=<offset>`. Conversations have no per-turn timeout; use `tasks` when you need a bounded reply window. 
     * Post a new caller message into the conversation.
     */
    async sendConversationMessageRaw(requestParameters: SendConversationMessageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SendMessageResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling sendConversationMessage().'
            );
        }

        if (requestParameters['convId'] == null) {
            throw new runtime.RequiredError(
                'convId',
                'Required parameter "convId" was null or undefined when calling sendConversationMessage().'
            );
        }

        if (requestParameters['sendMessageRequest'] == null) {
            throw new runtime.RequiredError(
                'sendMessageRequest',
                'Required parameter "sendMessageRequest" was null or undefined when calling sendConversationMessage().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{convId}/messages`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"convId"}}`, encodeURIComponent(String(requestParameters['convId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SendMessageRequestToJSON(requestParameters['sendMessageRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SendMessageResponseFromJSON(jsonValue));
    }

    /**
     * Non-blocking — returns 202 with the persisted `message_id`. The agent reply lands later via the SSE `/events` stream or a follow-up `GET /messages?since=<offset>`. Conversations have no per-turn timeout; use `tasks` when you need a bounded reply window. 
     * Post a new caller message into the conversation.
     */
    async sendConversationMessage(requestParameters: SendConversationMessageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SendMessageResponse> {
        const response = await this.sendConversationMessageRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Set conversation model override
     */
    async setConversationModelRaw(requestParameters: SetConversationModelOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling setConversationModel().'
            );
        }

        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling setConversationModel().'
            );
        }

        if (requestParameters['conversationId'] == null) {
            throw new runtime.RequiredError(
                'conversationId',
                'Required parameter "conversationId" was null or undefined when calling setConversationModel().'
            );
        }

        if (requestParameters['setConversationModelRequest'] == null) {
            throw new runtime.RequiredError(
                'setConversationModelRequest',
                'Required parameter "setConversationModelRequest" was null or undefined when calling setConversationModel().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (requestParameters['xBeeOSOperationId'] != null) {
            headerParameters['X-BeeOS-Operation-Id'] = String(requestParameters['xBeeOSOperationId']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{conversationId}/model`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"conversationId"}}`, encodeURIComponent(String(requestParameters['conversationId'])));

        const response = await this.request({
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: SetConversationModelRequestToJSON(requestParameters['setConversationModelRequest']),
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Set conversation model override
     */
    async setConversationModel(requestParameters: SetConversationModelOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.setConversationModelRaw(requestParameters, initOverrides);
    }

    /**
     * Same wire format as `tasks/{taskId}/events`. Unlike the task variant, the conversation event stream does NOT auto-close on agent reply — it stays open until the caller disconnects or the conversation is closed via `DELETE /conversations/{convId}`. 
     * SSE stream of new messages on the conversation.
     */
    async streamConversationEventsRaw(requestParameters: StreamConversationEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling streamConversationEvents().'
            );
        }

        if (requestParameters['convId'] == null) {
            throw new runtime.RequiredError(
                'convId',
                'Required parameter "convId" was null or undefined when calling streamConversationEvents().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/conversations/{convId}/events`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"convId"}}`, encodeURIComponent(String(requestParameters['convId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Same wire format as `tasks/{taskId}/events`. Unlike the task variant, the conversation event stream does NOT auto-close on agent reply — it stays open until the caller disconnects or the conversation is closed via `DELETE /conversations/{convId}`. 
     * SSE stream of new messages on the conversation.
     */
    async streamConversationEvents(requestParameters: StreamConversationEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.streamConversationEventsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const ListConversationsStateEnum = {
    Active: 'active',
    Open: 'open',
    Closed: 'closed',
    All: 'all'
} as const;
export type ListConversationsStateEnum = typeof ListConversationsStateEnum[keyof typeof ListConversationsStateEnum];
