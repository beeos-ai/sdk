/* tslint:disable */
/* eslint-disable */
/**
 * BeeOS OpenAPI (user contract)
 * The **BeeOS OpenAPI** user contract. The sole implementer is **[`services/openapi-gateway`](../services/openapi-gateway)** (`openapi-gw`, local `:8095`). SDKs (Go / TypeScript) published under [`sdks/beeos-ai-sdk-go`](../../sdks/beeos-ai-sdk-go) and [`sdks/beeos-ai-sdk`](../../sdks/beeos-ai-sdk) are generated directly from this document.  The main user Gateway (`services/gateway`, `:9080`) serves web / mobile / desktop clients and is **not** described by this document. It maintains its own route set independent of the SDK contract.  **Auth:** every operation requires either a user **JWT** or a **`oag_`** User API Key, both on `Authorization: Bearer`. Agent-level protocol endpoints (A2A JSON-RPC with `bak_`, MCP) live in [beeos-agent-integration-v1.yaml](beeos-agent-integration-v1.yaml) and are hosted by A2A Gateway / MCP Gateway — not by `openapi-gateway`.  ## Changelog  ### 1.1.0 (ADR-0022 — dual-layer message storage)  BREAKING for clients that depended on full streaming chunk replay via the persistent message log:  * `GET /api/v1/agents/{agentId}/conversations/{convId}/messages`   and `GET /api/v1/agents/{agentId}/tasks/{taskId}/messages` now   **default-filter out** ephemeral streaming chunks   (`agent_reply_delta`, `agent_thought_chunk`,   `agent_message_chunk`). Add `?include_deltas=true` to opt back in. * `latest_offset` continues to reflect the TRUE server-side max   offset across both filtered and unfiltered rows, so `since=` /   cursor-style pagination is unaffected. * SSE `/events` is **NOT** affected — live consumers continue to   receive every envelope as it happens. * Backwards-compatible recovery path: SSE `/events` may now emit   a `backfill_truncated` event frame when the client reconnects   with `Last-Event-ID` before the oldest retained chunk. SDKs   that don\'t recognise the frame should treat it as a hint to   resume from `oldest_redis_offset` instead (the field carrying   the smallest offset still readable on the per-channel stream). * **Clarified contract**: SSE event `offset` is strictly monotonic   per channel but **NOT guaranteed contiguous** (ADR-0022 §1.2).   Producer-side write failures may leave small holes; clients MUST   use `offset > since`, never `offset == since + 1`.  BREAKING for clients that depended on the `oag_` API key scope vocabulary:  * The per-route scope gate (`agents:read`, `agents:write`,   `tasks:read`, `tasks:write`, `files:read`, `files:write`,   `instances:read`, `instances:write`, plus the `admin:*` wildcard)   has been **removed entirely**. The 37 routes that previously   required scopes are now governed exclusively by owner-ACL — an   `oag_` User API Key inherits full access to its owner\'s resources. * The `403 insufficient_scope` error code is no longer emitted.   Existing callers that branched on it should fold the case into   their generic 403 / `forbidden` handler. * SDK clients that explicitly passed `scopes` to `createAPIKey`   should drop that argument. The Web / Desktop UIs no longer render   scope badges on existing keys. * Existing `oag_` keys continue to work unchanged — the owner_id   binding is preserved, and no key needs to be re-issued.  BREAKING for clients of `GET /api/v1/providers` that read the `capabilities` object on the wire:  * 7 of the 8 `capabilities` field names are now **camelCase**   (`longRunning`, `browserUse`, `codeExec`, `fileSystem`,   `customImage`, `maxDurationSec`, `costModel`) — matching this   OpenAPI contract. Previous wire emitted **snake_case**   (`long_running`, `browser_use`, …) because the server DTO bound   `capabilities` to the raw protobuf message   (`*pb.ProviderCapabilities`) whose auto-generated `json` tags   use snake_case. SDK consumers (`@beeos-ai/sdk`,   `github.com/beeos-ai/sdk-go`) generated from this spec were   **already unable** to read any `capabilities` field — the   camelCase property names produced by the generator never   matched the snake_case the server actually sent. Raw HTTP   callers that hand-coded against the old snake_case names must   migrate; SDK callers gain access to the field set for the first   time without code changes. * Note: the `device` boolean was always a single word — its wire   name (`device`) is unchanged. Only the seven multi-word   capability fields are affected by the rename. * `meta`, `id`, `name`, `description`, `version` are unchanged. * A new drift-guard (`internal/dto/contract_test.go`) now locks   the server DTO to the spec property names, so the two cannot   silently diverge again.  ### 1.0.0  Initial OpenAPI contract (ADR-001). 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ClickRequest,
  ComputerScreenshot200Response,
  ErrorResponse,
  GetComputerInfo200Response,
  InlineObject,
  KeyRequest,
  MoveRequest,
  ScrollRequest,
  TypeRequest,
} from '../models/index';
import {
    ClickRequestFromJSON,
    ClickRequestToJSON,
    ComputerScreenshot200ResponseFromJSON,
    ComputerScreenshot200ResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    GetComputerInfo200ResponseFromJSON,
    GetComputerInfo200ResponseToJSON,
    InlineObjectFromJSON,
    InlineObjectToJSON,
    KeyRequestFromJSON,
    KeyRequestToJSON,
    MoveRequestFromJSON,
    MoveRequestToJSON,
    ScrollRequestFromJSON,
    ScrollRequestToJSON,
    TypeRequestFromJSON,
    TypeRequestToJSON,
} from '../models/index';

export interface ComputerClickRequest {
    id: string;
    clickRequest: ClickRequest;
}

export interface ComputerKeyRequest {
    id: string;
    keyRequest: KeyRequest;
}

export interface ComputerMoveRequest {
    id: string;
    moveRequest: MoveRequest;
}

export interface ComputerScreenshotRequest {
    id: string;
}

export interface ComputerScrollRequest {
    id: string;
    scrollRequest: ScrollRequest;
}

export interface ComputerTypeRequest {
    id: string;
    typeRequest: TypeRequest;
}

export interface GetComputerInfoRequest {
    id: string;
}

/**
 * 
 */
export class ComputerApi extends runtime.BaseAPI {

    /**
     * Mouse click at (x, y).
     */
    async computerClickRaw(requestParameters: ComputerClickRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling computerClick().'
            );
        }

        if (requestParameters['clickRequest'] == null) {
            throw new runtime.RequiredError(
                'clickRequest',
                'Required parameter "clickRequest" was null or undefined when calling computerClick().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/computer/click`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ClickRequestToJSON(requestParameters['clickRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Mouse click at (x, y).
     */
    async computerClick(requestParameters: ComputerClickRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.computerClickRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Press a key (single) or hotkey combo (multiple keys).
     */
    async computerKeyRaw(requestParameters: ComputerKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling computerKey().'
            );
        }

        if (requestParameters['keyRequest'] == null) {
            throw new runtime.RequiredError(
                'keyRequest',
                'Required parameter "keyRequest" was null or undefined when calling computerKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/computer/key`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: KeyRequestToJSON(requestParameters['keyRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Press a key (single) or hotkey combo (multiple keys).
     */
    async computerKey(requestParameters: ComputerKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.computerKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Move the pointer to (x, y) without clicking.
     */
    async computerMoveRaw(requestParameters: ComputerMoveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling computerMove().'
            );
        }

        if (requestParameters['moveRequest'] == null) {
            throw new runtime.RequiredError(
                'moveRequest',
                'Required parameter "moveRequest" was null or undefined when calling computerMove().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/computer/move`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: MoveRequestToJSON(requestParameters['moveRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Move the pointer to (x, y) without clicking.
     */
    async computerMove(requestParameters: ComputerMoveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.computerMoveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Captures the current screen, stages it in BeeOS file storage, and returns a `file_id` plus a short-lived presigned download `url` — never inline base64. 
     * Capture a desktop screenshot (returns a file reference).
     */
    async computerScreenshotRaw(requestParameters: ComputerScreenshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ComputerScreenshot200Response>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling computerScreenshot().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/computer/screenshot`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ComputerScreenshot200ResponseFromJSON(jsonValue));
    }

    /**
     * Captures the current screen, stages it in BeeOS file storage, and returns a `file_id` plus a short-lived presigned download `url` — never inline base64. 
     * Capture a desktop screenshot (returns a file reference).
     */
    async computerScreenshot(requestParameters: ComputerScreenshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ComputerScreenshot200Response> {
        const response = await this.computerScreenshotRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Scroll in a cardinal direction.
     */
    async computerScrollRaw(requestParameters: ComputerScrollRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling computerScroll().'
            );
        }

        if (requestParameters['scrollRequest'] == null) {
            throw new runtime.RequiredError(
                'scrollRequest',
                'Required parameter "scrollRequest" was null or undefined when calling computerScroll().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/computer/scroll`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ScrollRequestToJSON(requestParameters['scrollRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Scroll in a cardinal direction.
     */
    async computerScroll(requestParameters: ComputerScrollRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.computerScrollRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Type Unicode text into the focused field.
     */
    async computerTypeRaw(requestParameters: ComputerTypeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling computerType().'
            );
        }

        if (requestParameters['typeRequest'] == null) {
            throw new runtime.RequiredError(
                'typeRequest',
                'Required parameter "typeRequest" was null or undefined when calling computerType().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/computer/type`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TypeRequestToJSON(requestParameters['typeRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Type Unicode text into the focused field.
     */
    async computerType(requestParameters: ComputerTypeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.computerTypeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the instance\'s online status, OS, current screen geometry (absolute device pixels), the supported action verbs, and the foreground app. Geometry is fetched live so it reflects the current display state. Returns 404 if the instance is an android device (use the `mobile` endpoints instead). 
     * Live desktop control info (geometry + supported actions).
     */
    async getComputerInfoRaw(requestParameters: GetComputerInfoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetComputerInfo200Response>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getComputerInfo().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/computer`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetComputerInfo200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns the instance\'s online status, OS, current screen geometry (absolute device pixels), the supported action verbs, and the foreground app. Geometry is fetched live so it reflects the current display state. Returns 404 if the instance is an android device (use the `mobile` endpoints instead). 
     * Live desktop control info (geometry + supported actions).
     */
    async getComputerInfo(requestParameters: GetComputerInfoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetComputerInfo200Response> {
        const response = await this.getComputerInfoRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
