/* tslint:disable */
/* eslint-disable */
/**
 * BeeOS OpenAPI (user contract)
 * The **BeeOS OpenAPI** user contract. The sole implementer is **[`services/openapi-gateway`](../services/openapi-gateway)** (`openapi-gw`, local `:8095`). SDKs (Go / TypeScript) published under [`sdks/beeos-ai-sdk-go`](../../sdks/beeos-ai-sdk-go) and [`sdks/beeos-ai-sdk`](../../sdks/beeos-ai-sdk) are generated directly from this document.  The main user Gateway (`services/gateway`, `:9080`) serves web / mobile / desktop clients and is **not** described by this document. It maintains its own route set independent of the SDK contract.  **Auth:** every operation requires either a user **JWT** or a **`oag_`** User API Key, both on `Authorization: Bearer`. Agent-level protocol endpoints (A2A JSON-RPC with `bak_`, MCP) live in [beeos-agent-integration-v1.yaml](beeos-agent-integration-v1.yaml) and are hosted by A2A Gateway / MCP Gateway — not by `openapi-gateway`.  ## Changelog  ### 1.1.0 (ADR-0022 — dual-layer message storage)  BREAKING for clients that depended on full streaming chunk replay via the persistent message log:  * `GET /api/v1/agents/{agentId}/conversations/{convId}/messages`   and `GET /api/v1/agents/{agentId}/tasks/{taskId}/messages` now   **default-filter out** ephemeral streaming chunks   (`agent_reply_delta`, `agent_thought_chunk`,   `agent_message_chunk`). Add `?include_deltas=true` to opt back in. * `latest_offset` continues to reflect the TRUE server-side max   offset across both filtered and unfiltered rows, so `since=` /   cursor-style pagination is unaffected. * SSE `/events` is **NOT** affected — live consumers continue to   receive every envelope as it happens. * Backwards-compatible recovery path: SSE `/events` may now emit   a `backfill_truncated` event frame when the client reconnects   with `Last-Event-ID` before the oldest retained chunk. SDKs   that don\'t recognise the frame should treat it as a hint to   resume from `oldest_redis_offset` instead (the field carrying   the smallest offset still readable on the per-channel stream). * **Clarified contract**: SSE event `offset` is strictly monotonic   per channel but **NOT guaranteed contiguous** (ADR-0022 §1.2).   Producer-side write failures may leave small holes; clients MUST   use `offset > since`, never `offset == since + 1`.  BREAKING for clients that depended on the `oag_` API key scope vocabulary:  * The per-route scope gate (`agents:read`, `agents:write`,   `tasks:read`, `tasks:write`, `files:read`, `files:write`,   `instances:read`, `instances:write`, plus the `admin:*` wildcard)   has been **removed entirely**. The 37 routes that previously   required scopes are now governed exclusively by owner-ACL — an   `oag_` User API Key inherits full access to its owner\'s resources. * The `403 insufficient_scope` error code is no longer emitted.   Existing callers that branched on it should fold the case into   their generic 403 / `forbidden` handler. * SDK clients that explicitly passed `scopes` to `createAPIKey`   should drop that argument. The Web / Desktop UIs no longer render   scope badges on existing keys. * Existing `oag_` keys continue to work unchanged — the owner_id   binding is preserved, and no key needs to be re-issued.  BREAKING for clients of `GET /api/v1/providers` that read the `capabilities` object on the wire:  * 7 of the 8 `capabilities` field names are now **camelCase**   (`longRunning`, `browserUse`, `codeExec`, `fileSystem`,   `customImage`, `maxDurationSec`, `costModel`) — matching this   OpenAPI contract. Previous wire emitted **snake_case**   (`long_running`, `browser_use`, …) because the server DTO bound   `capabilities` to the raw protobuf message   (`*pb.ProviderCapabilities`) whose auto-generated `json` tags   use snake_case. SDK consumers (`@beeos-ai/sdk`,   `github.com/beeos-ai/sdk-go`) generated from this spec were   **already unable** to read any `capabilities` field — the   camelCase property names produced by the generator never   matched the snake_case the server actually sent. Raw HTTP   callers that hand-coded against the old snake_case names must   migrate; SDK callers gain access to the field set for the first   time without code changes. * Note: the `device` boolean was always a single word — its wire   name (`device`) is unchanged. Only the seven multi-word   capability fields are affected by the rename. * `meta`, `id`, `name`, `description`, `version` are unchanged. * A new drift-guard (`internal/dto/contract_test.go`) now locks   the server DTO to the spec property names, so the two cannot   silently diverge again.  ### 1.0.0  Initial OpenAPI contract (ADR-001). 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ComputerScreenshot200Response,
  ErrorResponse,
  GetComputerInfo200Response,
  InlineObject,
  KeyRequest,
  PressButtonRequest,
  ScrollRequest,
  SwipeRequest,
  TapRequest,
  TypeRequest,
} from '../models/index';
import {
    ComputerScreenshot200ResponseFromJSON,
    ComputerScreenshot200ResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    GetComputerInfo200ResponseFromJSON,
    GetComputerInfo200ResponseToJSON,
    InlineObjectFromJSON,
    InlineObjectToJSON,
    KeyRequestFromJSON,
    KeyRequestToJSON,
    PressButtonRequestFromJSON,
    PressButtonRequestToJSON,
    ScrollRequestFromJSON,
    ScrollRequestToJSON,
    SwipeRequestFromJSON,
    SwipeRequestToJSON,
    TapRequestFromJSON,
    TapRequestToJSON,
    TypeRequestFromJSON,
    TypeRequestToJSON,
} from '../models/index';

export interface GetMobileInfoRequest {
    id: string;
}

export interface MobileKeyRequest {
    id: string;
    keyRequest: KeyRequest;
}

export interface MobilePressButtonRequest {
    id: string;
    pressButtonRequest: PressButtonRequest;
}

export interface MobileScreenshotRequest {
    id: string;
}

export interface MobileScrollRequest {
    id: string;
    scrollRequest: ScrollRequest;
}

export interface MobileSwipeRequest {
    id: string;
    swipeRequest: SwipeRequest;
}

export interface MobileTapRequest {
    id: string;
    tapRequest: TapRequest;
}

export interface MobileTypeRequest {
    id: string;
    typeRequest: TypeRequest;
}

/**
 * 
 */
export class MobileApi extends runtime.BaseAPI {

    /**
     * Returns the android instance\'s online status, screen geometry, supported actions, and foreground app. Returns 404 if the instance is a desktop device (use the `computer` endpoints).  This is the synchronous **control plane**: `online` + `screen_*` reflect the *live* control-channel state at request time. For the device\'s **registration / static** metadata (BYOD device type, model, OS, registered geometry, hardware capabilities) — readable even when the device is offline — use `GET /api/v1/instances/{id}/device/info` (`DeviceInfoDTO`) instead. 
     * Live mobile control info (geometry + supported actions).
     */
    async getMobileInfoRaw(requestParameters: GetMobileInfoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetComputerInfo200Response>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getMobileInfo().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/mobile`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetComputerInfo200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns the android instance\'s online status, screen geometry, supported actions, and foreground app. Returns 404 if the instance is a desktop device (use the `computer` endpoints).  This is the synchronous **control plane**: `online` + `screen_*` reflect the *live* control-channel state at request time. For the device\'s **registration / static** metadata (BYOD device type, model, OS, registered geometry, hardware capabilities) — readable even when the device is offline — use `GET /api/v1/instances/{id}/device/info` (`DeviceInfoDTO`) instead. 
     * Live mobile control info (geometry + supported actions).
     */
    async getMobileInfo(requestParameters: GetMobileInfoRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetComputerInfo200Response> {
        const response = await this.getMobileInfoRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Press an Android key (KeyCode name).
     */
    async mobileKeyRaw(requestParameters: MobileKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling mobileKey().'
            );
        }

        if (requestParameters['keyRequest'] == null) {
            throw new runtime.RequiredError(
                'keyRequest',
                'Required parameter "keyRequest" was null or undefined when calling mobileKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/mobile/key`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: KeyRequestToJSON(requestParameters['keyRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Press an Android key (KeyCode name).
     */
    async mobileKey(requestParameters: MobileKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.mobileKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Press a hardware / navigation button (back / home / ...).
     */
    async mobilePressButtonRaw(requestParameters: MobilePressButtonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling mobilePressButton().'
            );
        }

        if (requestParameters['pressButtonRequest'] == null) {
            throw new runtime.RequiredError(
                'pressButtonRequest',
                'Required parameter "pressButtonRequest" was null or undefined when calling mobilePressButton().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/mobile/press_button`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: PressButtonRequestToJSON(requestParameters['pressButtonRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Press a hardware / navigation button (back / home / ...).
     */
    async mobilePressButton(requestParameters: MobilePressButtonRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.mobilePressButtonRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Capture a mobile screenshot (returns a file reference).
     */
    async mobileScreenshotRaw(requestParameters: MobileScreenshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ComputerScreenshot200Response>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling mobileScreenshot().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/mobile/screenshot`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ComputerScreenshot200ResponseFromJSON(jsonValue));
    }

    /**
     * Capture a mobile screenshot (returns a file reference).
     */
    async mobileScreenshot(requestParameters: MobileScreenshotRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ComputerScreenshot200Response> {
        const response = await this.mobileScreenshotRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Scroll in a cardinal direction.
     */
    async mobileScrollRaw(requestParameters: MobileScrollRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling mobileScroll().'
            );
        }

        if (requestParameters['scrollRequest'] == null) {
            throw new runtime.RequiredError(
                'scrollRequest',
                'Required parameter "scrollRequest" was null or undefined when calling mobileScroll().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/mobile/scroll`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ScrollRequestToJSON(requestParameters['scrollRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Scroll in a cardinal direction.
     */
    async mobileScroll(requestParameters: MobileScrollRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.mobileScrollRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Swipe from (x1, y1) to (x2, y2).
     */
    async mobileSwipeRaw(requestParameters: MobileSwipeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling mobileSwipe().'
            );
        }

        if (requestParameters['swipeRequest'] == null) {
            throw new runtime.RequiredError(
                'swipeRequest',
                'Required parameter "swipeRequest" was null or undefined when calling mobileSwipe().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/mobile/swipe`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SwipeRequestToJSON(requestParameters['swipeRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Swipe from (x1, y1) to (x2, y2).
     */
    async mobileSwipe(requestParameters: MobileSwipeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.mobileSwipeRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Tap at (x, y).
     */
    async mobileTapRaw(requestParameters: MobileTapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling mobileTap().'
            );
        }

        if (requestParameters['tapRequest'] == null) {
            throw new runtime.RequiredError(
                'tapRequest',
                'Required parameter "tapRequest" was null or undefined when calling mobileTap().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/mobile/tap`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TapRequestToJSON(requestParameters['tapRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Tap at (x, y).
     */
    async mobileTap(requestParameters: MobileTapRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.mobileTapRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Type Unicode text into the focused field.
     */
    async mobileTypeRaw(requestParameters: MobileTypeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<InlineObject>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling mobileType().'
            );
        }

        if (requestParameters['typeRequest'] == null) {
            throw new runtime.RequiredError(
                'typeRequest',
                'Required parameter "typeRequest" was null or undefined when calling mobileType().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/instances/{id}/mobile/type`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: TypeRequestToJSON(requestParameters['typeRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => InlineObjectFromJSON(jsonValue));
    }

    /**
     * Type Unicode text into the focused field.
     */
    async mobileType(requestParameters: MobileTypeRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<InlineObject> {
        const response = await this.mobileTypeRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
