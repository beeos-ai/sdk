/* tslint:disable */
/* eslint-disable */
/**
 * BeeOS OpenAPI (user contract)
 * The **BeeOS OpenAPI** user contract. The sole implementer is **[`services/openapi-gateway`](../services/openapi-gateway)** (`openapi-gw`, local `:8095`). SDKs (Go / TypeScript) published under [`sdks/beeos-ai-sdk-go`](../../sdks/beeos-ai-sdk-go) and [`sdks/beeos-ai-sdk`](../../sdks/beeos-ai-sdk) are generated directly from this document.  The main user Gateway (`services/gateway`, `:9080`) serves web / mobile / desktop clients and is **not** described by this document. It maintains its own route set independent of the SDK contract.  **Auth:** every operation requires either a user **JWT** or a **`oag_`** User API Key, both on `Authorization: Bearer`. Agent-level protocol endpoints (A2A JSON-RPC with `bak_`, MCP) live in [beeos-agent-integration-v1.yaml](beeos-agent-integration-v1.yaml) and are hosted by A2A Gateway / MCP Gateway — not by `openapi-gateway`.  ## Changelog  ### 1.1.0 (ADR-0022 — dual-layer message storage)  BREAKING for clients that depended on full streaming chunk replay via the persistent message log:  * `GET /api/v1/agents/{agentId}/conversations/{convId}/messages`   and `GET /api/v1/agents/{agentId}/tasks/{taskId}/messages` now   **default-filter out** ephemeral streaming chunks   (`agent_reply_delta`, `agent_thought_chunk`,   `agent_message_chunk`). Add `?include_deltas=true` to opt back in. * `latest_offset` continues to reflect the TRUE server-side max   offset across both filtered and unfiltered rows, so `since=` /   cursor-style pagination is unaffected. * SSE `/events` is **NOT** affected — live consumers continue to   receive every envelope as it happens. * Backwards-compatible recovery path: SSE `/events` may now emit   a `backfill_truncated` event frame when the client reconnects   with `Last-Event-ID` before the oldest retained chunk. SDKs   that don\'t recognise the frame should treat it as a hint to   resume from `oldest_redis_offset` instead (the field carrying   the smallest offset still readable on the per-channel stream). * **Clarified contract**: SSE event `offset` is strictly monotonic   per channel but **NOT guaranteed contiguous** (ADR-0022 §1.2).   Producer-side write failures may leave small holes; clients MUST   use `offset > since`, never `offset == since + 1`.  BREAKING for clients that depended on the `oag_` API key scope vocabulary:  * The per-route scope gate (`agents:read`, `agents:write`,   `tasks:read`, `tasks:write`, `files:read`, `files:write`,   `instances:read`, `instances:write`, plus the `admin:*` wildcard)   has been **removed entirely**. The 37 routes that previously   required scopes are now governed exclusively by owner-ACL — an   `oag_` User API Key inherits full access to its owner\'s resources. * The `403 insufficient_scope` error code is no longer emitted.   Existing callers that branched on it should fold the case into   their generic 403 / `forbidden` handler. * SDK clients that explicitly passed `scopes` to `createAPIKey`   should drop that argument. The Web / Desktop UIs no longer render   scope badges on existing keys. * Existing `oag_` keys continue to work unchanged — the owner_id   binding is preserved, and no key needs to be re-issued.  BREAKING for clients of `GET /api/v1/providers` that read the `capabilities` object on the wire:  * 7 of the 8 `capabilities` field names are now **camelCase**   (`longRunning`, `browserUse`, `codeExec`, `fileSystem`,   `customImage`, `maxDurationSec`, `costModel`) — matching this   OpenAPI contract. Previous wire emitted **snake_case**   (`long_running`, `browser_use`, …) because the server DTO bound   `capabilities` to the raw protobuf message   (`*pb.ProviderCapabilities`) whose auto-generated `json` tags   use snake_case. SDK consumers (`@beeos-ai/sdk`,   `github.com/beeos-ai/sdk-go`) generated from this spec were   **already unable** to read any `capabilities` field — the   camelCase property names produced by the generator never   matched the snake_case the server actually sent. Raw HTTP   callers that hand-coded against the old snake_case names must   migrate; SDK callers gain access to the field set for the first   time without code changes. * Note: the `device` boolean was always a single word — its wire   name (`device`) is unchanged. Only the seven multi-word   capability fields are affected by the rename. * `meta`, `id`, `name`, `description`, `version` are unchanged. * A new drift-guard (`internal/dto/contract_test.go`) now locks   the server DTO to the spec property names, so the two cannot   silently diverge again.  ### 1.0.0  Initial OpenAPI contract (ADR-001). 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CancelTaskRequest,
  ContinueTaskRequest,
  CreateTaskRequest,
  ErrorResponse,
  ListMessagesResponse,
  ListTaskWebhooksResponse,
  ListTasksResponse,
  ListWebhookDeliveriesResponse,
  RedeliverWebhook202Response,
  RegisterTaskWebhookRequest,
  TaskCreatedResponse,
  TaskResponse,
  TaskWebhookResponse,
} from '../models/index';
import {
    CancelTaskRequestFromJSON,
    CancelTaskRequestToJSON,
    ContinueTaskRequestFromJSON,
    ContinueTaskRequestToJSON,
    CreateTaskRequestFromJSON,
    CreateTaskRequestToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ListMessagesResponseFromJSON,
    ListMessagesResponseToJSON,
    ListTaskWebhooksResponseFromJSON,
    ListTaskWebhooksResponseToJSON,
    ListTasksResponseFromJSON,
    ListTasksResponseToJSON,
    ListWebhookDeliveriesResponseFromJSON,
    ListWebhookDeliveriesResponseToJSON,
    RedeliverWebhook202ResponseFromJSON,
    RedeliverWebhook202ResponseToJSON,
    RegisterTaskWebhookRequestFromJSON,
    RegisterTaskWebhookRequestToJSON,
    TaskCreatedResponseFromJSON,
    TaskCreatedResponseToJSON,
    TaskResponseFromJSON,
    TaskResponseToJSON,
    TaskWebhookResponseFromJSON,
    TaskWebhookResponseToJSON,
} from '../models/index';

export interface CancelTaskOperationRequest {
    agentId: string;
    taskId: string;
    cancelTaskRequest?: CancelTaskRequest;
}

export interface ContinueTaskOperationRequest {
    agentId: string;
    taskId: string;
    continueTaskRequest?: ContinueTaskRequest;
}

export interface CreateTaskOperationRequest {
    agentId: string;
    createTaskRequest: CreateTaskRequest;
}

export interface DeleteTaskWebhookRequest {
    agentId: string;
    taskId: string;
    webhookId: string;
}

export interface GetTaskRequest {
    agentId: string;
    taskId: string;
}

export interface ListAgentTasksRequest {
    agentId: string;
    state?: ListAgentTasksStateEnum;
    since?: string;
    limit?: number;
}

export interface ListTaskMessagesRequest {
    agentId: string;
    taskId: string;
    since?: number;
    limit?: number;
    includeDeltas?: boolean;
}

export interface ListTaskWebhooksRequest {
    agentId: string;
    taskId: string;
}

export interface ListUserTasksRequest {
    agentId?: string;
    instanceId?: string;
    state?: ListUserTasksStateEnum;
    since?: string;
    limit?: number;
}

export interface ListWebhookDeliveriesRequest {
    agentId: string;
    taskId: string;
    webhookId: string;
    limit?: number;
}

export interface RedeliverWebhookRequest {
    agentId: string;
    taskId: string;
    webhookId: string;
    deliveryId: string;
}

export interface RegisterTaskWebhookOperationRequest {
    agentId: string;
    taskId: string;
    registerTaskWebhookRequest: RegisterTaskWebhookRequest;
}

export interface StreamTaskEventsRequest {
    agentId: string;
    taskId: string;
    since?: number;
}

/**
 * 
 */
export class TasksApi extends runtime.BaseAPI {

    /**
     * Emits a `chat_cancel` envelope so the agent can abort its in-flight turn cleanly, then closes the channel with `reason=canceled`. Idempotent — re-issuing cancel on an already-canceled task is a no-op and still returns the snapshot. 
     * Cancel an in-flight task.
     */
    async cancelTaskRaw(requestParameters: CancelTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TaskResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling cancelTask().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling cancelTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/cancel`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CancelTaskRequestToJSON(requestParameters['cancelTaskRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TaskResponseFromJSON(jsonValue));
    }

    /**
     * Emits a `chat_cancel` envelope so the agent can abort its in-flight turn cleanly, then closes the channel with `reason=canceled`. Idempotent — re-issuing cancel on an already-canceled task is a no-op and still returns the snapshot. 
     * Cancel an in-flight task.
     */
    async cancelTask(requestParameters: CancelTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TaskResponse> {
        const response = await this.cancelTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Publishes a `user.continue` (or `user.auth_grant` when `auth_grant=true`) envelope so the agent can resume from a paused `input_required` / `auth_required` state. Returns the snapshot AFTER the envelope is published — typically `running`, or still `input_required` if the agent hasn\'t processed the resume yet. 
     * Resume a task from input_required / auth_required.
     */
    async continueTaskRaw(requestParameters: ContinueTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TaskResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling continueTask().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling continueTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/continue`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ContinueTaskRequestToJSON(requestParameters['continueTaskRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TaskResponseFromJSON(jsonValue));
    }

    /**
     * Publishes a `user.continue` (or `user.auth_grant` when `auth_grant=true`) envelope so the agent can resume from a paused `input_required` / `auth_required` state. Returns the snapshot AFTER the envelope is published — typically `running`, or still `input_required` if the agent hasn\'t processed the resume yet. 
     * Resume a task from input_required / auth_required.
     */
    async continueTask(requestParameters: ContinueTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TaskResponse> {
        const response = await this.continueTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates a single-shot task channel and publishes the initial `chat_message` envelope, then returns 202 with the task_id. The agent reply lands asynchronously — callers either poll `GET /tasks/{taskId}` or subscribe to the SSE event stream at `GET /tasks/{taskId}/events`.  The channel inherits the caller-supplied `deadline_ms` (capped at 7 days server-side). If the deadline elapses before the agent produces a terminal message the task transitions to `timeout`. 
     * Submit an async task to an agent.
     */
    async createTaskRaw(requestParameters: CreateTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TaskCreatedResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling createTask().'
            );
        }

        if (requestParameters['createTaskRequest'] == null) {
            throw new runtime.RequiredError(
                'createTaskRequest',
                'Required parameter "createTaskRequest" was null or undefined when calling createTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateTaskRequestToJSON(requestParameters['createTaskRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TaskCreatedResponseFromJSON(jsonValue));
    }

    /**
     * Creates a single-shot task channel and publishes the initial `chat_message` envelope, then returns 202 with the task_id. The agent reply lands asynchronously — callers either poll `GET /tasks/{taskId}` or subscribe to the SSE event stream at `GET /tasks/{taskId}/events`.  The channel inherits the caller-supplied `deadline_ms` (capped at 7 days server-side). If the deadline elapses before the agent produces a terminal message the task transitions to `timeout`. 
     * Submit an async task to an agent.
     */
    async createTask(requestParameters: CreateTaskOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TaskCreatedResponse> {
        const response = await this.createTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Unsubscribe a webhook.
     */
    async deleteTaskWebhookRaw(requestParameters: DeleteTaskWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling deleteTaskWebhook().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling deleteTaskWebhook().'
            );
        }

        if (requestParameters['webhookId'] == null) {
            throw new runtime.RequiredError(
                'webhookId',
                'Required parameter "webhookId" was null or undefined when calling deleteTaskWebhook().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/webhooks/{webhookId}`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));
        urlPath = urlPath.replace(`{${"webhookId"}}`, encodeURIComponent(String(requestParameters['webhookId'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Unsubscribe a webhook.
     */
    async deleteTaskWebhook(requestParameters: DeleteTaskWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteTaskWebhookRaw(requestParameters, initOverrides);
    }

    /**
     * Returns the snapshot derived by `msgderive` from the task\'s channel-message log — safe to call at any cadence; the resolver is a pure function and terminal states short-circuit polling loops. 
     * Get a task\'s current state snapshot.
     */
    async getTaskRaw(requestParameters: GetTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TaskResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling getTask().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling getTask().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TaskResponseFromJSON(jsonValue));
    }

    /**
     * Returns the snapshot derived by `msgderive` from the task\'s channel-message log — safe to call at any cadence; the resolver is a pure function and terminal states short-circuit polling loops. 
     * Get a task\'s current state snapshot.
     */
    async getTask(requestParameters: GetTaskRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TaskResponse> {
        const response = await this.getTaskRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the caller\'s tasks against this agent, filtered server-side by participation (the caller\'s `user:<id>`) and client-side by `target_agent_id == {agentId}` AND `protocol == \"openapi\"` so the endpoint stays scoped to OpenAPI-created tasks (excludes conversations and other-protocol channels).  Performance note: the wire DTO is INTENTIONALLY lightweight — it does NOT carry the derived `status` / `result` / `error` because computing those requires a per-task message-log scan (up to 1000 messages each inside `GetTask`). Callers that need the precise status follow up with `GET /tasks/{taskId}` per row, or subscribe to SSE `GET /tasks/{taskId}/events`. 
     * List the caller\'s tasks for this agent.
     */
    async listAgentTasksRaw(requestParameters: ListAgentTasksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTasksResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling listAgentTasks().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['state'] != null) {
            queryParameters['state'] = requestParameters['state'];
        }

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListTasksResponseFromJSON(jsonValue));
    }

    /**
     * Returns the caller\'s tasks against this agent, filtered server-side by participation (the caller\'s `user:<id>`) and client-side by `target_agent_id == {agentId}` AND `protocol == \"openapi\"` so the endpoint stays scoped to OpenAPI-created tasks (excludes conversations and other-protocol channels).  Performance note: the wire DTO is INTENTIONALLY lightweight — it does NOT carry the derived `status` / `result` / `error` because computing those requires a per-task message-log scan (up to 1000 messages each inside `GetTask`). Callers that need the precise status follow up with `GET /tasks/{taskId}` per row, or subscribe to SSE `GET /tasks/{taskId}/events`. 
     * List the caller\'s tasks for this agent.
     */
    async listAgentTasks(requestParameters: ListAgentTasksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTasksResponse> {
        const response = await this.listAgentTasksRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the channel-message log for a task — `chat_message`, `agent_reply`, `chat_cancel`, `user.continue`, etc. By default EXCLUDES ephemeral streaming chunks (`agent_reply_delta`, `agent_thought_chunk`, `agent_message_chunk`); set `include_deltas=true` to receive them. `latest_offset` always reflects the true server-side max offset so resuming with `since=<latest_offset>` is safe regardless of the filter.  Same wire shape as `GET /conversations/{convId}/messages` because a task\'s `task_id` IS the underlying conversation_id (a task is just a single-shot channel with a deadline).  Auth path is intentionally cheaper than `getTask` (one MS `GetConversation` + metadata check, no full message-log scan) so paged reads stay O(1)-per-page in auth cost. 
     * List the persisted message log for a task (paged by offset).
     */
    async listTaskMessagesRaw(requestParameters: ListTaskMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListMessagesResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling listTaskMessages().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling listTaskMessages().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['includeDeltas'] != null) {
            queryParameters['include_deltas'] = requestParameters['includeDeltas'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/messages`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListMessagesResponseFromJSON(jsonValue));
    }

    /**
     * Returns the channel-message log for a task — `chat_message`, `agent_reply`, `chat_cancel`, `user.continue`, etc. By default EXCLUDES ephemeral streaming chunks (`agent_reply_delta`, `agent_thought_chunk`, `agent_message_chunk`); set `include_deltas=true` to receive them. `latest_offset` always reflects the true server-side max offset so resuming with `since=<latest_offset>` is safe regardless of the filter.  Same wire shape as `GET /conversations/{convId}/messages` because a task\'s `task_id` IS the underlying conversation_id (a task is just a single-shot channel with a deadline).  Auth path is intentionally cheaper than `getTask` (one MS `GetConversation` + metadata check, no full message-log scan) so paged reads stay O(1)-per-page in auth cost. 
     * List the persisted message log for a task (paged by offset).
     */
    async listTaskMessages(requestParameters: ListTaskMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListMessagesResponse> {
        const response = await this.listTaskMessagesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * List registered webhooks for a task.
     */
    async listTaskWebhooksRaw(requestParameters: ListTaskWebhooksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTaskWebhooksResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling listTaskWebhooks().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling listTaskWebhooks().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/webhooks`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListTaskWebhooksResponseFromJSON(jsonValue));
    }

    /**
     * List registered webhooks for a task.
     */
    async listTaskWebhooks(requestParameters: ListTaskWebhooksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTaskWebhooksResponse> {
        const response = await this.listTaskWebhooksRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Cross-agent task list (P2-B). Returns every task channel the caller owns, newest first — the unified \"my tasks\" view for SDKs that need to render a multi-agent inbox without fanning out per-agent.  Same wire shape and `next_since` pagination as the per-agent variant at `GET /api/v1/agents/{agentId}/tasks`. Filtering an existing per-agent call is equivalent to passing `agent_id` explicitly.  Authorization: the caller\'s `owner_id` is the only ACL gate. Tasks fan out across agents the caller has invoked, whether or not they still have visibility on those agents today — possession of the task row itself proves access at submit time.  Only `protocol=openapi` channels surface here. Long-running conversations live at `GET /api/v1/agents/{agentId}/conversations` and are not mixed in to keep status semantics stable (tasks have a deadline; conversations don\'t). 
     * List all of the caller\'s tasks across every agent.
     */
    async listUserTasksRaw(requestParameters: ListUserTasksRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListTasksResponse>> {
        const queryParameters: any = {};

        if (requestParameters['agentId'] != null) {
            queryParameters['agent_id'] = requestParameters['agentId'];
        }

        if (requestParameters['instanceId'] != null) {
            queryParameters['instance_id'] = requestParameters['instanceId'];
        }

        if (requestParameters['state'] != null) {
            queryParameters['state'] = requestParameters['state'];
        }

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/tasks`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListTasksResponseFromJSON(jsonValue));
    }

    /**
     * Cross-agent task list (P2-B). Returns every task channel the caller owns, newest first — the unified \"my tasks\" view for SDKs that need to render a multi-agent inbox without fanning out per-agent.  Same wire shape and `next_since` pagination as the per-agent variant at `GET /api/v1/agents/{agentId}/tasks`. Filtering an existing per-agent call is equivalent to passing `agent_id` explicitly.  Authorization: the caller\'s `owner_id` is the only ACL gate. Tasks fan out across agents the caller has invoked, whether or not they still have visibility on those agents today — possession of the task row itself proves access at submit time.  Only `protocol=openapi` channels surface here. Long-running conversations live at `GET /api/v1/agents/{agentId}/conversations` and are not mixed in to keep status semantics stable (tasks have a deadline; conversations don\'t). 
     * List all of the caller\'s tasks across every agent.
     */
    async listUserTasks(requestParameters: ListUserTasksRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListTasksResponse> {
        const response = await this.listUserTasksRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Returns the most recent per-attempt rows for this webhook subscription, newest first. P2-A — durable delivery audit log + retry queue.  Each row carries the lifecycle status (`pending` / `succeeded` / `failed` / `dead_letter`), the most recent HTTP status code, the truncated last error, and the scheduled time of the next attempt while the row is still in-flight. Sensitive fields — the rendered payload bytes, the optional bearer `token`, and the HMAC `secret` — are NEVER returned.  Use this endpoint when:   * a callback didn\'t arrive and you need to confirm     whether BeeOS attempted it (`status=failed` /     `dead_letter` with a populated `last_error`);   * you\'re reconciling a missed webhook against     `GET /tasks/{id}` and want a definitive audit trail;   * you intend to manually replay a dead-lettered row via     `POST .../redeliver`. 
     * List webhook delivery attempts (audit log).
     */
    async listWebhookDeliveriesRaw(requestParameters: ListWebhookDeliveriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListWebhookDeliveriesResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling listWebhookDeliveries().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling listWebhookDeliveries().'
            );
        }

        if (requestParameters['webhookId'] == null) {
            throw new runtime.RequiredError(
                'webhookId',
                'Required parameter "webhookId" was null or undefined when calling listWebhookDeliveries().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/webhooks/{webhookId}/deliveries`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));
        urlPath = urlPath.replace(`{${"webhookId"}}`, encodeURIComponent(String(requestParameters['webhookId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListWebhookDeliveriesResponseFromJSON(jsonValue));
    }

    /**
     * Returns the most recent per-attempt rows for this webhook subscription, newest first. P2-A — durable delivery audit log + retry queue.  Each row carries the lifecycle status (`pending` / `succeeded` / `failed` / `dead_letter`), the most recent HTTP status code, the truncated last error, and the scheduled time of the next attempt while the row is still in-flight. Sensitive fields — the rendered payload bytes, the optional bearer `token`, and the HMAC `secret` — are NEVER returned.  Use this endpoint when:   * a callback didn\'t arrive and you need to confirm     whether BeeOS attempted it (`status=failed` /     `dead_letter` with a populated `last_error`);   * you\'re reconciling a missed webhook against     `GET /tasks/{id}` and want a definitive audit trail;   * you intend to manually replay a dead-lettered row via     `POST .../redeliver`. 
     * List webhook delivery attempts (audit log).
     */
    async listWebhookDeliveries(requestParameters: ListWebhookDeliveriesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListWebhookDeliveriesResponse> {
        const response = await this.listWebhookDeliveriesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Clones a terminal-failure or dead-lettered delivery row into a fresh `pending` row that the background dispatcher will pick up on its next poll. The original row is preserved for audit. The returned `WebhookDeliveryResponse` is the new pending row — poll the delivery list or watch the callback URL itself to observe outcome.  The source row MUST be in status `failed` or `dead_letter`; replaying `pending` or `succeeded` rows returns `409 conflict` with code `conflict.invalid_state`. The clone replays the EXACT payload bytes the receiver should have seen the first time — HMAC signatures, headers, body — so receivers can treat redeliver attempts identically to original attempts.  Caveat: each redeliver call enqueues a NEW pending row, so repeated clicks produce repeated deliveries. This endpoint is for manual rescue, not for caller-side retry-on-network-error. 
     * Manually replay a webhook delivery.
     */
    async redeliverWebhookRaw(requestParameters: RedeliverWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<RedeliverWebhook202Response>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling redeliverWebhook().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling redeliverWebhook().'
            );
        }

        if (requestParameters['webhookId'] == null) {
            throw new runtime.RequiredError(
                'webhookId',
                'Required parameter "webhookId" was null or undefined when calling redeliverWebhook().'
            );
        }

        if (requestParameters['deliveryId'] == null) {
            throw new runtime.RequiredError(
                'deliveryId',
                'Required parameter "deliveryId" was null or undefined when calling redeliverWebhook().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/webhooks/{webhookId}/deliveries/{deliveryId}/redeliver`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));
        urlPath = urlPath.replace(`{${"webhookId"}}`, encodeURIComponent(String(requestParameters['webhookId'])));
        urlPath = urlPath.replace(`{${"deliveryId"}}`, encodeURIComponent(String(requestParameters['deliveryId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => RedeliverWebhook202ResponseFromJSON(jsonValue));
    }

    /**
     * Clones a terminal-failure or dead-lettered delivery row into a fresh `pending` row that the background dispatcher will pick up on its next poll. The original row is preserved for audit. The returned `WebhookDeliveryResponse` is the new pending row — poll the delivery list or watch the callback URL itself to observe outcome.  The source row MUST be in status `failed` or `dead_letter`; replaying `pending` or `succeeded` rows returns `409 conflict` with code `conflict.invalid_state`. The clone replays the EXACT payload bytes the receiver should have seen the first time — HMAC signatures, headers, body — so receivers can treat redeliver attempts identically to original attempts.  Caveat: each redeliver call enqueues a NEW pending row, so repeated clicks produce repeated deliveries. This endpoint is for manual rescue, not for caller-side retry-on-network-error. 
     * Manually replay a webhook delivery.
     */
    async redeliverWebhook(requestParameters: RedeliverWebhookRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<RedeliverWebhook202Response> {
        const response = await this.redeliverWebhookRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Subscribes a caller-supplied HTTPS endpoint to receive POST callbacks when the task reaches a terminal state (`completed` / `failed` / `canceled` / `timeout` / `rejected`). Idempotent on `(task_id, url)` — re-registering the same URL updates the token.  Implementation note: openapi-gateway is zero-DB; the underlying subscription is stored by A2A Service in its `webhooks` table. The webhook fires regardless of which protocol created the task (OpenAPI / A2A / MCP) — same row, different `protocol_filter`. 
     * Register a webhook callback for terminal task events.
     */
    async registerTaskWebhookRaw(requestParameters: RegisterTaskWebhookOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TaskWebhookResponse>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling registerTaskWebhook().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling registerTaskWebhook().'
            );
        }

        if (requestParameters['registerTaskWebhookRequest'] == null) {
            throw new runtime.RequiredError(
                'registerTaskWebhookRequest',
                'Required parameter "registerTaskWebhookRequest" was null or undefined when calling registerTaskWebhook().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/webhooks`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: RegisterTaskWebhookRequestToJSON(requestParameters['registerTaskWebhookRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TaskWebhookResponseFromJSON(jsonValue));
    }

    /**
     * Subscribes a caller-supplied HTTPS endpoint to receive POST callbacks when the task reaches a terminal state (`completed` / `failed` / `canceled` / `timeout` / `rejected`). Idempotent on `(task_id, url)` — re-registering the same URL updates the token.  Implementation note: openapi-gateway is zero-DB; the underlying subscription is stored by A2A Service in its `webhooks` table. The webhook fires regardless of which protocol created the task (OpenAPI / A2A / MCP) — same row, different `protocol_filter`. 
     * Register a webhook callback for terminal task events.
     */
    async registerTaskWebhook(requestParameters: RegisterTaskWebhookOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TaskWebhookResponse> {
        const response = await this.registerTaskWebhookRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * SSE proxy onto the underlying MS conversation. Each event is `message` (JSON payload mirroring the on-channel envelope: `type`, `message_id`, `offset`, `in_reply_to`, `publisher_id`, `payload`, `created_at`).  The stream auto-closes when the channel reaches a terminal envelope (`agent_reply` / `agent_reply_error`) — clients should release the connection on the synthesized `end` event. 
     * Subscribe to a task\'s event stream (SSE).
     */
    async streamTaskEventsRaw(requestParameters: StreamTaskEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        if (requestParameters['agentId'] == null) {
            throw new runtime.RequiredError(
                'agentId',
                'Required parameter "agentId" was null or undefined when calling streamTaskEvents().'
            );
        }

        if (requestParameters['taskId'] == null) {
            throw new runtime.RequiredError(
                'taskId',
                'Required parameter "taskId" was null or undefined when calling streamTaskEvents().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['since'] != null) {
            queryParameters['since'] = requestParameters['since'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/agents/{agentId}/tasks/{taskId}/events`;
        urlPath = urlPath.replace(`{${"agentId"}}`, encodeURIComponent(String(requestParameters['agentId'])));
        urlPath = urlPath.replace(`{${"taskId"}}`, encodeURIComponent(String(requestParameters['taskId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * SSE proxy onto the underlying MS conversation. Each event is `message` (JSON payload mirroring the on-channel envelope: `type`, `message_id`, `offset`, `in_reply_to`, `publisher_id`, `payload`, `created_at`).  The stream auto-closes when the channel reaches a terminal envelope (`agent_reply` / `agent_reply_error`) — clients should release the connection on the synthesized `end` event. 
     * Subscribe to a task\'s event stream (SSE).
     */
    async streamTaskEvents(requestParameters: StreamTaskEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.streamTaskEventsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const ListAgentTasksStateEnum = {
    Active: 'active',
    Open: 'open',
    Closed: 'closed',
    All: 'all'
} as const;
export type ListAgentTasksStateEnum = typeof ListAgentTasksStateEnum[keyof typeof ListAgentTasksStateEnum];
/**
 * @export
 */
export const ListUserTasksStateEnum = {
    Active: 'active',
    Open: 'open',
    Closed: 'closed',
    All: 'all'
} as const;
export type ListUserTasksStateEnum = typeof ListUserTasksStateEnum[keyof typeof ListUserTasksStateEnum];
