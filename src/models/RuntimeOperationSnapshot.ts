/* tslint:disable */
/* eslint-disable */
/**
 * BeeOS OpenAPI (user contract)
 * The **BeeOS OpenAPI** user contract. The sole implementer is **[`services/openapi-gateway`](../services/openapi-gateway)** (`openapi-gw`, local `:8095`). SDKs (Go / TypeScript) published under [`sdks/beeos-ai-sdk-go`](../../sdks/beeos-ai-sdk-go) and [`sdks/beeos-ai-sdk`](../../sdks/beeos-ai-sdk) are generated directly from this document.  The main user Gateway (`services/gateway`, `:9080`) serves web / mobile / desktop clients and is **not** described by this document. It maintains its own route set independent of the SDK contract.  **Auth:** every operation requires either a user **JWT** or a **`oag_`** User API Key, both on `Authorization: Bearer`. Agent-level protocol endpoints (A2A JSON-RPC with `bak_`, MCP) live in [beeos-agent-integration-v1.yaml](beeos-agent-integration-v1.yaml) and are hosted by A2A Gateway / MCP Gateway — not by `openapi-gateway`.  ## Changelog  ### 1.1.0 (ADR-0022 — dual-layer message storage)  BREAKING for clients that depended on full streaming chunk replay via the persistent message log:  * `GET /api/v1/agents/{agentId}/conversations/{convId}/messages`   and `GET /api/v1/agents/{agentId}/tasks/{taskId}/messages` now   **default-filter out** ephemeral streaming chunks   (`agent_reply_delta`, `agent_thought_chunk`,   `agent_message_chunk`). Add `?include_deltas=true` to opt back in. * `latest_offset` continues to reflect the TRUE server-side max   offset across both filtered and unfiltered rows, so `since=` /   cursor-style pagination is unaffected. * SSE `/events` is **NOT** affected — live consumers continue to   receive every envelope as it happens. * Backwards-compatible recovery path: SSE `/events` may now emit   a `backfill_truncated` event frame when the client reconnects   with `Last-Event-ID` before the oldest retained chunk. SDKs   that don\'t recognise the frame should treat it as a hint to   resume from `oldest_redis_offset` instead (the field carrying   the smallest offset still readable on the per-channel stream). * **Clarified contract**: SSE event `offset` is strictly monotonic   per channel but **NOT guaranteed contiguous** (ADR-0022 §1.2).   Producer-side write failures may leave small holes; clients MUST   use `offset > since`, never `offset == since + 1`.  BREAKING for clients that depended on the `oag_` API key scope vocabulary:  * The per-route scope gate (`agents:read`, `agents:write`,   `tasks:read`, `tasks:write`, `files:read`, `files:write`,   `instances:read`, `instances:write`, plus the `admin:*` wildcard)   has been **removed entirely**. The 37 routes that previously   required scopes are now governed exclusively by owner-ACL — an   `oag_` User API Key inherits full access to its owner\'s resources. * The `403 insufficient_scope` error code is no longer emitted.   Existing callers that branched on it should fold the case into   their generic 403 / `forbidden` handler. * SDK clients that explicitly passed `scopes` to `createAPIKey`   should drop that argument. The Web / Desktop UIs no longer render   scope badges on existing keys. * Existing `oag_` keys continue to work unchanged — the owner_id   binding is preserved, and no key needs to be re-issued.  BREAKING for clients of `GET /api/v1/providers` that read the `capabilities` object on the wire:  * 7 of the 8 `capabilities` field names are now **camelCase**   (`longRunning`, `browserUse`, `codeExec`, `fileSystem`,   `customImage`, `maxDurationSec`, `costModel`) — matching this   OpenAPI contract. Previous wire emitted **snake_case**   (`long_running`, `browser_use`, …) because the server DTO bound   `capabilities` to the raw protobuf message   (`*pb.ProviderCapabilities`) whose auto-generated `json` tags   use snake_case. SDK consumers (`@beeos-ai/sdk`,   `github.com/beeos-ai/sdk-go`) generated from this spec were   **already unable** to read any `capabilities` field — the   camelCase property names produced by the generator never   matched the snake_case the server actually sent. Raw HTTP   callers that hand-coded against the old snake_case names must   migrate; SDK callers gain access to the field set for the first   time without code changes. * Note: the `device` boolean was always a single word — its wire   name (`device`) is unchanged. Only the seven multi-word   capability fields are affected by the rename. * `meta`, `id`, `name`, `description`, `version` are unchanged. * A new drift-guard (`internal/dto/contract_test.go`) now locks   the server DTO to the spec property names, so the two cannot   silently diverge again.  ### 1.0.0  Initial OpenAPI contract (ADR-001). 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { RuntimeOperationTarget } from './RuntimeOperationTarget';
import {
    RuntimeOperationTargetFromJSON,
    RuntimeOperationTargetFromJSONTyped,
    RuntimeOperationTargetToJSON,
    RuntimeOperationTargetToJSONTyped,
} from './RuntimeOperationTarget';
import type { RuntimeOperationError } from './RuntimeOperationError';
import {
    RuntimeOperationErrorFromJSON,
    RuntimeOperationErrorFromJSONTyped,
    RuntimeOperationErrorToJSON,
    RuntimeOperationErrorToJSONTyped,
} from './RuntimeOperationError';
import type { RuntimeOperationSnapshotCancelRequest } from './RuntimeOperationSnapshotCancelRequest';
import {
    RuntimeOperationSnapshotCancelRequestFromJSON,
    RuntimeOperationSnapshotCancelRequestFromJSONTyped,
    RuntimeOperationSnapshotCancelRequestToJSON,
    RuntimeOperationSnapshotCancelRequestToJSONTyped,
} from './RuntimeOperationSnapshotCancelRequest';
import type { RuntimeOperationSnapshotProgress } from './RuntimeOperationSnapshotProgress';
import {
    RuntimeOperationSnapshotProgressFromJSON,
    RuntimeOperationSnapshotProgressFromJSONTyped,
    RuntimeOperationSnapshotProgressToJSON,
    RuntimeOperationSnapshotProgressToJSONTyped,
} from './RuntimeOperationSnapshotProgress';
import type { ServiceRuntimeMethod } from './ServiceRuntimeMethod';
import {
    ServiceRuntimeMethodFromJSON,
    ServiceRuntimeMethodFromJSONTyped,
    ServiceRuntimeMethodToJSON,
    ServiceRuntimeMethodToJSONTyped,
} from './ServiceRuntimeMethod';
import type { RuntimeOperationSnapshotProjection } from './RuntimeOperationSnapshotProjection';
import {
    RuntimeOperationSnapshotProjectionFromJSON,
    RuntimeOperationSnapshotProjectionFromJSONTyped,
    RuntimeOperationSnapshotProjectionToJSON,
    RuntimeOperationSnapshotProjectionToJSONTyped,
} from './RuntimeOperationSnapshotProjection';

/**
 * 
 * @export
 * @interface RuntimeOperationSnapshot
 */
export interface RuntimeOperationSnapshot {
    [key: string]: any | any;
    /**
     * 
     * @type {string}
     * @memberof RuntimeOperationSnapshot
     */
    id: string;
    /**
     * 
     * @type {string}
     * @memberof RuntimeOperationSnapshot
     */
    instanceId: string;
    /**
     * 
     * @type {RuntimeOperationTarget}
     * @memberof RuntimeOperationSnapshot
     */
    target: RuntimeOperationTarget;
    /**
     * 
     * @type {ServiceRuntimeMethod}
     * @memberof RuntimeOperationSnapshot
     */
    method: ServiceRuntimeMethod;
    /**
     * 
     * @type {string}
     * @memberof RuntimeOperationSnapshot
     */
    capability: RuntimeOperationSnapshotCapabilityEnum;
    /**
     * 
     * @type {string}
     * @memberof RuntimeOperationSnapshot
     */
    transport: RuntimeOperationSnapshotTransportEnum;
    /**
     * 
     * @type {string}
     * @memberof RuntimeOperationSnapshot
     */
    sequence: string;
    /**
     * 
     * @type {string}
     * @memberof RuntimeOperationSnapshot
     */
    cursor?: string;
    /**
     * 
     * @type {Date}
     * @memberof RuntimeOperationSnapshot
     */
    createdAt: Date;
    /**
     * 
     * @type {Date}
     * @memberof RuntimeOperationSnapshot
     */
    updatedAt: Date;
    /**
     * 
     * @type {string}
     * @memberof RuntimeOperationSnapshot
     */
    status: RuntimeOperationSnapshotStatusEnum;
    /**
     * 
     * @type {string}
     * @memberof RuntimeOperationSnapshot
     */
    effectState: RuntimeOperationSnapshotEffectStateEnum;
    /**
     * 
     * @type {boolean}
     * @memberof RuntimeOperationSnapshot
     */
    terminal: boolean;
    /**
     * 
     * @type {RuntimeOperationSnapshotProgress}
     * @memberof RuntimeOperationSnapshot
     */
    progress?: RuntimeOperationSnapshotProgress;
    /**
     * 
     * @type {RuntimeOperationSnapshotCancelRequest}
     * @memberof RuntimeOperationSnapshot
     */
    cancelRequest?: RuntimeOperationSnapshotCancelRequest;
    /**
     * 
     * @type {RuntimeOperationSnapshotProjection}
     * @memberof RuntimeOperationSnapshot
     */
    projection?: RuntimeOperationSnapshotProjection;
    /**
     * 
     * @type {any}
     * @memberof RuntimeOperationSnapshot
     */
    result?: any | null;
    /**
     * 
     * @type {RuntimeOperationError}
     * @memberof RuntimeOperationSnapshot
     */
    error?: RuntimeOperationError;
}


/**
 * @export
 */
export const RuntimeOperationSnapshotCapabilityEnum = {
    AgentsLifecycle: 'agents.lifecycle',
    Skills: 'skills',
    Models: 'models',
    Cron: 'cron',
    Mcp: 'mcp',
    ConversationControl: 'conversation.control'
} as const;
export type RuntimeOperationSnapshotCapabilityEnum = typeof RuntimeOperationSnapshotCapabilityEnum[keyof typeof RuntimeOperationSnapshotCapabilityEnum];

/**
 * @export
 */
export const RuntimeOperationSnapshotTransportEnum = {
    Service: 'service'
} as const;
export type RuntimeOperationSnapshotTransportEnum = typeof RuntimeOperationSnapshotTransportEnum[keyof typeof RuntimeOperationSnapshotTransportEnum];

/**
 * @export
 */
export const RuntimeOperationSnapshotStatusEnum = {
    Queued: 'queued',
    Running: 'running',
    RuntimeCommitted: 'runtime_committed',
    ProjectionPending: 'projection_pending',
    ProjectionBlocked: 'projection_blocked',
    Succeeded: 'succeeded',
    Failed: 'failed',
    Cancelled: 'cancelled',
    Expired: 'expired',
    OutcomeUnknown: 'outcome_unknown'
} as const;
export type RuntimeOperationSnapshotStatusEnum = typeof RuntimeOperationSnapshotStatusEnum[keyof typeof RuntimeOperationSnapshotStatusEnum];

/**
 * @export
 */
export const RuntimeOperationSnapshotEffectStateEnum = {
    Queued: 'queued',
    Running: 'running',
    Committed: 'committed',
    Failed: 'failed',
    Cancelled: 'cancelled',
    Expired: 'expired',
    OutcomeUnknown: 'outcome_unknown'
} as const;
export type RuntimeOperationSnapshotEffectStateEnum = typeof RuntimeOperationSnapshotEffectStateEnum[keyof typeof RuntimeOperationSnapshotEffectStateEnum];


/**
 * Check if a given object implements the RuntimeOperationSnapshot interface.
 */
export function instanceOfRuntimeOperationSnapshot(value: object): value is RuntimeOperationSnapshot {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('instanceId' in value) || value['instanceId'] === undefined) return false;
    if (!('target' in value) || value['target'] === undefined) return false;
    if (!('method' in value) || value['method'] === undefined) return false;
    if (!('capability' in value) || value['capability'] === undefined) return false;
    if (!('transport' in value) || value['transport'] === undefined) return false;
    if (!('sequence' in value) || value['sequence'] === undefined) return false;
    if (!('createdAt' in value) || value['createdAt'] === undefined) return false;
    if (!('updatedAt' in value) || value['updatedAt'] === undefined) return false;
    if (!('status' in value) || value['status'] === undefined) return false;
    if (!('effectState' in value) || value['effectState'] === undefined) return false;
    if (!('terminal' in value) || value['terminal'] === undefined) return false;
    return true;
}

export function RuntimeOperationSnapshotFromJSON(json: any): RuntimeOperationSnapshot {
    return RuntimeOperationSnapshotFromJSONTyped(json, false);
}

export function RuntimeOperationSnapshotFromJSONTyped(json: any, ignoreDiscriminator: boolean): RuntimeOperationSnapshot {
    if (json == null) {
        return json;
    }
    return {
        
            ...json,
        'id': json['id'],
        'instanceId': json['instanceId'],
        'target': RuntimeOperationTargetFromJSON(json['target']),
        'method': ServiceRuntimeMethodFromJSON(json['method']),
        'capability': json['capability'],
        'transport': json['transport'],
        'sequence': json['sequence'],
        'cursor': json['cursor'] == null ? undefined : json['cursor'],
        'createdAt': (new Date(json['createdAt'])),
        'updatedAt': (new Date(json['updatedAt'])),
        'status': json['status'],
        'effectState': json['effectState'],
        'terminal': json['terminal'],
        'progress': json['progress'] == null ? undefined : RuntimeOperationSnapshotProgressFromJSON(json['progress']),
        'cancelRequest': json['cancelRequest'] == null ? undefined : RuntimeOperationSnapshotCancelRequestFromJSON(json['cancelRequest']),
        'projection': json['projection'] == null ? undefined : RuntimeOperationSnapshotProjectionFromJSON(json['projection']),
        'result': json['result'] == null ? undefined : json['result'],
        'error': json['error'] == null ? undefined : RuntimeOperationErrorFromJSON(json['error']),
    };
}

export function RuntimeOperationSnapshotToJSON(json: any): RuntimeOperationSnapshot {
    return RuntimeOperationSnapshotToJSONTyped(json, false);
}

export function RuntimeOperationSnapshotToJSONTyped(value?: RuntimeOperationSnapshot | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
            ...value,
        'id': value['id'],
        'instanceId': value['instanceId'],
        'target': RuntimeOperationTargetToJSON(value['target']),
        'method': ServiceRuntimeMethodToJSON(value['method']),
        'capability': value['capability'],
        'transport': value['transport'],
        'sequence': value['sequence'],
        'cursor': value['cursor'],
        'createdAt': ((value['createdAt']).toISOString()),
        'updatedAt': ((value['updatedAt']).toISOString()),
        'status': value['status'],
        'effectState': value['effectState'],
        'terminal': value['terminal'],
        'progress': RuntimeOperationSnapshotProgressToJSON(value['progress']),
        'cancelRequest': RuntimeOperationSnapshotCancelRequestToJSON(value['cancelRequest']),
        'projection': RuntimeOperationSnapshotProjectionToJSON(value['projection']),
        'result': value['result'],
        'error': RuntimeOperationErrorToJSON(value['error']),
    };
}

